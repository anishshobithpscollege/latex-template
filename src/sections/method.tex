\section{Method}
\label{sec:method}
Describe the approach. Inline math looks like $E = mc^2$, and a
numbered equation looks like this:
\begin{equation}
  \label{eq:gaussian}
  f(x) = \frac{1}{\sigma\sqrt{2\pi}}\, e^{-\frac{(x-\mu)^2}{2\sigma^2}}.
\end{equation}
Refer back to it with Equation~\ref{eq:gaussian}.

\begin{definition}
A statement you want to set apart from the surrounding text.
\end{definition}

\subsection{Code}
Drop in a code listing when you need one:
\begin{lstlisting}[language=Python, caption={A short example.}]
def greet(name: str) -> str:
    # return a greeting
    return f"Hello, {name}!"
\end{lstlisting}
